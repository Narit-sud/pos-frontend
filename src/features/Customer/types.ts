export interface CustomerType {
    uuid: string;
    name: string;
    surname: string;
    phoneNumber: string;
    email: string;
    detail: string;
    createdAt: string;
    updatedAt: string;
}
